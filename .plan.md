# Plan: Solve Known Gaps

## Gap 1: Upgrade `practice-results.tsx` to V2 with Question Type Breakdown — ALREADY DONE
- `app/practice-results.tsx` already uses `legacyBatchToV2()`, `getCorrectId()`, and has full `typeStats` analytics
- Renders "QUESTION TYPES" breakdown section in the Analytics tab
- Functionally equivalent to `chapter-results.tsx` — no work needed

## Gap 3: Saved Questions Viewer — ALREADY DONE
- `app/(main)/bookmarks.tsx` is fully implemented (327 lines)
- Tab bar in `app/(main)/_layout.tsx` already has a "Saved" tab routing to it
- No work needed here

---

## Gap 2: "Practice My Mistakes" Mode

### Batch 1 (3 files):

**File 1: `app/practice-mistakes.tsx`** (NEW)
- New screen that receives wrong question IDs from a completed exam session
- Receives `sessionId` + `mode` (chapter|practice) as route params
- Extracts wrong-answer IDs from the corresponding history entry in Redux
- Calls `getV2QuestionsByIds(wrongIds)` to load questions
- Presents them in a simple quiz format using `QuestionRenderer`
- Tracks answers locally (no Redux session, just local state)
- Shows mini-results at the end (correct/total, retry option)
- VaNi journal aesthetic matching existing screens

**File 2: `app/practice-results.tsx`** (EDIT)
- Add a "Practice My Mistakes" button in the actions section (after "Review Answers")
- Only show when `wrongNum > 0`
- Routes to `/practice-mistakes` with `sessionId` + `mode=practice`

**File 3: `app/chapter-results.tsx`** (EDIT)
- Add a "Practice My Mistakes" button in the actions section (after "Review Answers")
- Only show when `wrongNum > 0`
- Routes to `/practice-mistakes` with `sessionId` + `mode=chapter`

---

## Execution Order

1. **Gap 2 Batch 1** — Create `practice-mistakes.tsx` + wire buttons in both results screens
